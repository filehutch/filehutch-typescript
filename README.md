# @assethutch/sdk

TypeScript client for [AssetHutch](https://assethutch.com), file infrastructure for apps that
aren't Netflix.

Your app persists an opaque file id (`file_…`). AssetHutch owns uploads, private files, signed
URLs, delivery, and named image transforms. Your storage, or AssetHutch's, sits behind it.
